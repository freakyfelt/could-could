{
  "name": "@freakyfelt/could-could",
  "version": "1.0.0",
  "description": "An authorization library built on top of JsonLogic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "format": "prettier -w lib test",
    "release": "npm run test && npm run format && npm run build",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajv": "^8.11.0",
    "json-logic-js": "^2.0.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.16.11",
    "@babel/preset-typescript": "^7.16.7",
    "@tsconfig/node17": "^1.0.0",
    "@types/jest": "^29.0.3",
    "@types/json-logic-js": "^2.0.1",
    "jest": "^29.1.1",
    "prettier": "^2.6.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
